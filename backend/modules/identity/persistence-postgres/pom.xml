<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.niv.payment</groupId>
        <artifactId>identity</artifactId>
        <version>0.1.0-SNAPSHOT</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <artifactId>identity-persistence-postgres</artifactId>
    <name>identity-persistence-postgres</name>
    <description>Identity-owned jOOQ PostgreSQL adapter with schema migrations</description>

    <dependencies>
        <dependency>
            <groupId>com.niv.payment</groupId>
            <artifactId>identity-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.jooq</groupId>
            <artifactId>jooq</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-tx</artifactId>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <profiles>
        <profile>
            <id>jooq-codegen</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.flywaydb</groupId>
                        <artifactId>flyway-maven-plugin</artifactId>
                        <version>${flyway.version}</version>
                        <configuration>
                            <url>${env.JOOQ_CODEGEN_DB_URL}</url>
                            <user>${env.JOOQ_CODEGEN_DB_USERNAME}</user>
                            <password>${env.JOOQ_CODEGEN_DB_PASSWORD}</password>
                            <locations>
                                <location>filesystem:${project.basedir}/src/main/resources/db/migration</location>
                            </locations>
                            <cleanDisabled>true</cleanDisabled>
                        </configuration>
                        <executions>
                            <execution>
                                <id>migrate-jooq-codegen-database</id>
                                <phase>generate-sources</phase>
                                <goals>
                                    <goal>migrate</goal>
                                </goals>
                            </execution>
                        </executions>
                        <dependencies>
                            <dependency>
                                <groupId>org.flywaydb</groupId>
                                <artifactId>flyway-database-postgresql</artifactId>
                                <version>${flyway.version}</version>
                            </dependency>
                            <dependency>
                                <groupId>org.postgresql</groupId>
                                <artifactId>postgresql</artifactId>
                                <version>${postgresql.version}</version>
                            </dependency>
                        </dependencies>
                    </plugin>
                    <plugin>
                        <groupId>org.jooq</groupId>
                        <artifactId>jooq-codegen-maven</artifactId>
                        <version>${jooq.version}</version>
                        <configuration>
                            <jdbc>
                                <driver>org.postgresql.Driver</driver>
                                <url>${env.JOOQ_CODEGEN_DB_URL}</url>
                                <user>${env.JOOQ_CODEGEN_DB_USERNAME}</user>
                                <password>${env.JOOQ_CODEGEN_DB_PASSWORD}</password>
                            </jdbc>
                            <generator>
                                <database>
                                    <name>org.jooq.meta.postgres.PostgresDatabase</name>
                                    <inputSchema>public</inputSchema>
                                    <includes>iam_.*</includes>
                                    <excludes>flyway_schema_history</excludes>
                                    <includeTables>true</includeTables>
                                    <includeSequences>true</includeSequences>
                                    <includeIndexes>true</includeIndexes>
                                    <includeRoutines>false</includeRoutines>
                                    <includeTriggers>false</includeTriggers>
                                </database>
                                <generate>
                                    <deprecated>false</deprecated>
                                    <relations>true</relations>
                                    <records>true</records>
                                    <pojos>false</pojos>
                                    <daos>false</daos>
                                    <javaTimeTypes>true</javaTimeTypes>
                                </generate>
                                <target>
                                    <packageName>com.niv.payment.permission.persistence.jooq.generated</packageName>
                                    <directory>${project.basedir}/src/main/java</directory>
                                    <clean>true</clean>
                                </target>
                            </generator>
                        </configuration>
                        <executions>
                            <execution>
                                <id>generate-jooq-model</id>
                                <phase>generate-sources</phase>
                                <goals>
                                    <goal>generate</goal>
                                </goals>
                            </execution>
                        </executions>
                        <dependencies>
                            <dependency>
                                <groupId>org.postgresql</groupId>
                                <artifactId>postgresql</artifactId>
                                <version>${postgresql.version}</version>
                            </dependency>
                        </dependencies>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
